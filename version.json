{
  "version": "chat-v116-fix-history-loss-race",
  "notes": [
    "修复聊天记录会莫名变少：IndexedDB 全量加载完成前禁止写权威存储，避免启动 race 期间自动保存用 40 条 localStorage 摘要覆盖并删除全量历史",
    "保存动作不再冒充 IndexedDB 已加载标志，损坏不会被固化；仅当 IndexedDB 确为空时才用摘要迁移写入",
    "IndexedDB 不可用时不再静默截断，提示用户历史无法完整持久化"
  ]
}
